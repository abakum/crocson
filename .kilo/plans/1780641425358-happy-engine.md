# Этап 4: Упростить open_url_android.go и get_mime_android.go

## 1. Добавить Java-методы в GoNativeActivity.java

```java
static void openIntent(String intentStr) {
    try {
        android.content.Intent intent = android.content.Intent.parseUri(intentStr, android.content.Intent.URI_INTENT_SCHEME);
        goNativeActivity.startActivity(intent);
    } catch (Exception e) {
        Log.e(TAG, "Java: openIntent failed: " + e.getMessage());
    }
}

static String getMimeType(String uriStr) {
    try {
        android.net.Uri uri = android.net.Uri.parse(uriStr);
        // Сначала пробуем ContentResolver.getType
        String type = goNativeActivity.getContentResolver().getType(uri);
        if (type != null) return type;
        // Затем query через mime_type column
        String[] projection = {"mime_type"};
        android.database.Cursor cursor = goNativeActivity.getContentResolver().query(uri, projection, null, null, null);
        if (cursor != null) {
            try {
                if (cursor.moveToFirst()) {
                    return cursor.getString(0);
                }
            } finally {
                cursor.close();
            }
        }
    } catch (Exception e) {
        Log.e(TAG, "Java: getMimeType failed: " + e.getMessage());
    }
    return null;
}
```

## 2. Переписать open_url_android.go (140→~40 строк)

Удалить весь C-блок, заменить на:
```go
func OpenURL(intentStr string) error {
	return callVoidString("openIntent", intentStr)
}
```
Оставить `OpenDAV` как есть.

## 3. Переписать get_mime_android.go (219→~20 строк)

Удалить весь C-блок, заменить на:
```go
func mimeType(uri fyne.URI) string {
	if uri == nil {
		return ""
	}
	s, _ := callStringString("getMimeType", uri.String())
	return s
}

func MimeType(uri fyne.URI) string {
	if uri == nil {
		return ""
	}
	s, _ := callStringString("getMimeType", uri.String())
	return s
}
```

## 4. Сборка
```
make wsl install arm64
```

---

# Этап 5: Перенести Go-функции в call_static_android.go

Перенести содержимое 5 файлов (без CGo) в конец `call_static_android.go`, файлы удалить:

| Файл | Строк | Что переносится |
|------|-------|-----------------|
| `caffeinate_android.go` | 54 | `apiLevel`, `caffeinate`, `SleepAllowed`, `startForegroundService`, `stopForegroundService` + `sleepCounter` |
| `get_mime_android.go` | 15 | `mimeType`, `MimeType` |
| `open_url_android.go` | 28 | `OpenURL`, `OpenDAV` |
| `resolver_android.go` | 52 | `CanList`, `uriBase`, `base`, `replace` |
| `toast_android.go` | 37 | `Toast` struct + все методы |

Удалить 5 файлов.

---

# ВЫПОЛНЕНО РАНЕЕ

## Этап 1: Удаление мёртвого кода ✅
- Удалены: `notification_android.go`, `exclude_from_recents_android.go`, `finish_android.go`, `set_result_android.go`
- Удалены: `start()`, `acquireWakeLock`/`releaseWakeLock`, заглушки в `for_android0.go`
- ~700 строк

## Этап 2: JNI-обёртки + перенос в Java ✅
- Добавлены в `call_static_android.go`: `callInt`, `callStringString`, `callBooleanString`, `callVoidInt`
- Добавлены в `GoNativeActivity.java`: `getApiLevel`, `getFileName`, `canListDirectory`, `setResultCode`
- Упрощены: `caffeinate_android.go` (101→48), `resolver_android.go` (348→58)
- ~500 строк C удалено

## Этап 3: Удалить мёртвые файлы ✅
- Удалены: `view_android.go` (283), `start_activity_android.go` (117), `open_app_info_android.go` (103), `open_app_settings_android.go` (113), `set_mod_time_android.go` (430), `get_flags_android.go` (274)
- Упрощён: `read_android.go` (590→44) — удалён весь C-блок + `BufferedDocumentReader`
- Удалены заглушки из `for_android0.go`
- `count_children_android.go` сохранён — `countChild()` используется внутри живого `IsDirectory()`
- ~1900 строк

---

# ОСТАТОК: живые файлы с inline C

## Быстрые победы с существующими обёртками

| Файл | Строк C / всего | Что нужно | Обёртка |
|------|----------------|-----------|---------|
| `open_url_android.go` | 55 / 140 | `callVoidString("openIntent", str)` | `callVoidString` |
| `get_mime_android.go` | 153 / 219 | `callStringString("getMimeType", uri)` | `callStringString` |

## Нужны новые JNI-обёртки

| Файл | Строк C / всего | Что нужно | Обёртка |
|------|----------------|-----------|---------|
| `download_android.go` | 653 / 742 | `callStringString2` (2 строковых аргумента), `callInt` (заменить `get_api_level`) | новый `callStringString2` |
| `get_children_uri_android.go` | 436 / 527 | DocumentsContract, возврат String | `callStringString` |
| `get_children_list_android.go` | 393 / 463 | ContentResolver.query → String[] | новый `callStringArray` |
| `documents_android.go` | 209 / 294 | DocumentsContract queries | — |
| `child_android.go` | 196 / 288 | DocumentsContract.child | — |
| `get_size_android.go` | 160 / 203 | ContentResolver → long | новый `callLongString` |
| `get_mod_time_android.go` | 128 / 197 | ContentResolver → long | новый `callLongString` |
| `media_android.go` | 116 / 290 | MediaStore queries | — |
| `count_children_android.go` | 229 / 327 | ContentResolver → COUNT | — |

## Не JNI (не переносится)

| Файл | Строк | Описание |
|------|-------|----------|
| `log_android.go` | 7 / 21 | NDK `__android_log_write` |
