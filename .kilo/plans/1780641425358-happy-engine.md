# Текущий этап: Fix 1c — убрать tree fallback из getFileName

## Fix 1c: `getFileName` — точно как в оригинальном C, без tree fallback

**Файл:** `GoNativeActivity.java` — `getFileName` теперь только DISPLAY_NAME query (проверено).

**Файл:** `call_static_android.go` — `base()` исправлен пользователем:
- `:` заменяется на `/` (было: на `_` через `replace()`)
- `%3A` заменяется на `/` в error-ветке `PathUnescape`
- `lastIndex("/")` теперь корректно извлекает последний сегмент для MiXplorer (`C739-3779:MAX` → `MAX`)

**Статус:** ПРИМЕНЕНО пользователем. Проверено.

## Fix 1b: добавить логирование ModTime перед Chtimes

**Файл:** `send.go:468-472` — добавить лог до условия:
```go
if err == nil {
    t, err := ModTime(source.URI())
    log.Debugf("ModTime %s: %v %v", source.URI(), t, err)
    if err == nil && !t.IsZero() {
        log.Debugf("Chtimes %s %v: %v", destination.Name(), t,
            os.Chtimes(destination.Name(), time.Time{}, t))
    }
}
```

**Файл:** `progress.go:342-346` — аналогично:
```go
if err == nil {
    t, err := fileModTime(source.Name())
    log.Debugf("fileModTime %s: %v %v", source.Name(), t, err)
    if err == nil && !t.IsZero() {
        log.Debugf("Chtimes %s %v: %v", destination.Name(), t,
            os.Chtimes(destination.Name(), time.Time{}, t))
    }
}
```

**Порядок (как в оригинальном C):**
1. `DISPLAY_NAME` query **первым** — работает для дочерних document URI
2. Если cursor вернул null/empty И URI — tree root (`/tree/` без `/document/`) → `getTreeDocumentId` → extract after last `:`
3. return null → Go fallback `base(uri.Path())`

## Фикс 2: `getModTime` — свой try/catch для каждого query

**Файл:** `GoNativeActivity.java` — заменить метод `getModTime`:

```java
static long getModTime(String uriStr) {
    try {
        android.net.Uri uri = android.net.Uri.parse(uriStr);
        android.content.ContentResolver resolver = goNativeActivity.getContentResolver();
        try {
            String[] projection = {"last_modified"};
            android.database.Cursor cursor = resolver.query(uri, projection, null, null, null);
            if (cursor != null) {
                try {
                    if (cursor.moveToFirst()) return cursor.getLong(0);
                } finally { cursor.close(); }
            }
        } catch (Exception ignored) {}
        try {
            android.database.Cursor cursor = resolver.query(uri, new String[]{"date_modified"}, null, null, null);
            if (cursor != null) {
                try {
                    if (cursor.moveToFirst()) return cursor.getLong(0);
                } finally { cursor.close(); }
            }
        } catch (Exception ignored) {}
    } catch (Exception e) {
        Log.e(TAG, "Java: getModTime failed: " + e.getMessage());
    }
    return -1;
}
```

## Фикс 3: `countChildren` — `buildChildDocumentsUri` (без UsingTree) во 2-м fallback

**Файл:** `GoNativeActivity.java` — заменить метод `countChildren`:

```java
static int countChildren(String uriStr) {
    try {
        android.net.Uri uri = android.net.Uri.parse(uriStr);
        android.content.ContentResolver resolver = goNativeActivity.getContentResolver();
        android.net.Uri childUri = null;
        try {
            String treeDocId = android.provider.DocumentsContract.getTreeDocumentId(uri);
            childUri = android.provider.DocumentsContract.buildChildDocumentsUriUsingTree(uri, treeDocId);
        } catch (Exception e1) {
            try {
                String docId = android.provider.DocumentsContract.getDocumentId(uri);
                childUri = android.provider.DocumentsContract.buildChildDocumentsUri(uri, docId);
            } catch (Exception e2) {
                childUri = uri;
            }
        }
        android.database.Cursor cursor = resolver.query(childUri, null, null, null, null);
        if (cursor != null) {
            try {
                return cursor.getCount();
            } finally { cursor.close(); }
        }
    } catch (Exception e) {
        Log.e(TAG, "Java: countChildren failed: " + e.getMessage());
    }
    return -1;
}
```

## Фикс 4+5: `getChildrenURIs` — 3 подхода к cursor + 3 fallback для child URI

**Файл:** `GoNativeActivity.java` — заменить метод `getChildrenURIs` + добавить `processChildrenCursor`:

```java
static String getChildrenURIs(String uriStr) {
    try {
        android.net.Uri uri = android.net.Uri.parse(uriStr);
        android.content.ContentResolver resolver = goNativeActivity.getContentResolver();
        android.net.Uri childUri = null;
        try {
            String treeDocId = android.provider.DocumentsContract.getTreeDocumentId(uri);
            childUri = android.provider.DocumentsContract.buildChildDocumentsUriUsingTree(uri, treeDocId);
            android.database.Cursor testCursor = resolver.query(childUri, new String[]{"_display_name", "document_id"}, null, null, null);
            if (testCursor != null) { testCursor.close(); }
            else {
                testCursor = resolver.query(childUri, null, null, null, null);
                if (testCursor != null) { testCursor.close(); }
                else { childUri = null; }
            }
        } catch (Exception e1) {
            childUri = null;
            try {
                String docId = android.provider.DocumentsContract.getDocumentId(uri);
                childUri = android.provider.DocumentsContract.buildChildDocumentsUri(uri, docId);
                android.database.Cursor testCursor = resolver.query(childUri, new String[]{"document_id"}, null, null, null);
                if (testCursor != null) { testCursor.close(); }
                else { childUri = null; }
            } catch (Exception e2) {
                childUri = null;
                try {
                    android.database.Cursor fallbackCursor = android.provider.DocumentsContract.getChildrenDocuments(resolver, uri);
                    if (fallbackCursor != null) return processChildrenCursor(fallbackCursor, uri);
                } catch (Exception e3) {}
                return "";
            }
        }
        if (childUri == null) return "";
        android.database.Cursor cursor = resolver.query(childUri, new String[]{"document_id"}, null, null, null);
        if (cursor == null) return "";
        return processChildrenCursor(cursor, uri);
    } catch (Exception e) {
        Log.e(TAG, "Java: getChildrenURIs failed: " + e.getMessage());
    }
    return "";
}

private static String processChildrenCursor(android.database.Cursor cursor, android.net.Uri treeUri) {
    StringBuilder sb = new StringBuilder();
    try {
        int docIdCol = cursor.getColumnIndex("document_id");
        if (docIdCol < 0) docIdCol = 0;
        boolean first = true;
        while (cursor.moveToNext()) {
            String docId = cursor.getString(docIdCol);
            if (docId != null) {
                android.net.Uri childDocUri = null;
                try {
                    childDocUri = android.provider.DocumentsContract.buildDocumentUriUsingTree(treeUri, docId);
                } catch (Exception ignored) {}
                if (childDocUri == null) {
                    try {
                        childDocUri = android.provider.DocumentsContract.buildChildDocumentsUriUsingTree(treeUri, docId);
                    } catch (Exception ignored) {}
                }
                if (childDocUri == null) {
                    try {
                        childDocUri = treeUri.buildUpon().appendPath(docId).build();
                    } catch (Exception ignored) {}
                }
                if (childDocUri != null) {
                    if (!first) sb.append("|");
                    sb.append(childDocUri.toString());
                    first = false;
                }
            }
        }
    } finally { cursor.close(); }
    return sb.toString();
}
```

## Фикс 6: `createFileInTree` — вернуть текст ошибки из exception

**Файл:** `GoNativeActivity.java` — заменить метод `createFileInTree`:

```java
static String createFileInTree(String treeUri, String fileName, String mimeType) {
    try {
        if (mimeType == null || mimeType.isEmpty()) mimeType = "application/octet-stream";
        android.net.Uri uri = android.net.Uri.parse(treeUri);
        android.content.ContentResolver resolver = goNativeActivity.getContentResolver();
        String treeDocId = android.provider.DocumentsContract.getTreeDocumentId(uri);
        android.net.Uri childDocsUri = android.provider.DocumentsContract.buildChildDocumentsUriUsingTree(uri, treeDocId);
        android.net.Uri newFileUri = android.provider.DocumentsContract.createDocument(resolver, childDocsUri, mimeType, fileName);
        if (newFileUri != null) return newFileUri.toString();
        return "error: createDocument returned null";
    } catch (Exception e) {
        String msg = e.getMessage();
        return msg != null ? "error: " + msg : "error: createFileInTree failed";
    }
}
```

## Фикс 7: `openIntent` — вернуть boolean вместо void

**Файл:** `GoNativeActivity.java` — заменить метод `openIntent`:

```java
static boolean openIntent(String intentStr) {
    try {
        android.content.Intent intent = android.content.Intent.parseUri(intentStr, android.content.Intent.URI_INTENT_SCHEME);
        goNativeActivity.startActivity(intent);
        return true;
    } catch (Exception e) {
        Log.e(TAG, "Java: openIntent failed: " + e.getMessage());
        return false;
    }
}
```

**Файл:** `call_static_android.go` — заменить Go-обёртку `OpenURL`:

```go
func OpenURL(intentStr string) error {
	ok, err := callBooleanString("openIntent", intentStr)
	if err != nil {
		return fmt.Errorf("intent failed: %s: %w", intentStr, err)
	}
	if !ok {
		return fmt.Errorf("intent failed: %s", intentStr)
	}
	return nil
}
```

# Следующий этап: восстановить setModTime для content:// URI

## Проблема

При сохранении из корзины в content:// destination модификация файла сбрасывается.
`progress.go:470-477` закомментирован, `setModTime` удалён в Stage 3.

Лог:
```
recv.go:430: copy /data/.../recv/IMG.jpg /document/primary%3ADownload%2FA%2FIMG.jpg
recv.go:244: remove file /data/.../recv/IMG.jpg
```
Никакого `Chtimes` или `setModTime` — mod time теряется.

## План

### 1. Java: `setModTime` — ContentResolver.update подход

**Файл:** `GoNativeActivity.java` — добавить static метод:

```java
static boolean setModTime(String uriStr, long modTimeMillis) {
    try {
        android.net.Uri uri = android.net.Uri.parse(uriStr);
        android.content.ContentResolver resolver = goNativeActivity.getContentResolver();
        android.content.ContentValues values = new android.content.ContentValues();
        String[] columns = {"last_modified", "date_modified"};
        for (String col : columns) {
            try {
                values.clear();
                values.put(col, modTimeMillis);
                int rows = resolver.update(uri, values, null, null);
                if (rows > 0) return true;
            } catch (Exception ignored) {}
        }
    } catch (Exception e) {
        Log.e(TAG, "Java: setModTime failed: " + e.getMessage());
    }
    return false;
}
```

### 2. C: `futimens` fallback — native wrapper

**Файл:** `call_static_android.go` — добавить C wrapper в existing CGo block:

```c
static jboolean setModTimeUsingFD(JNIEnv* env, jobject context, const char* uriStr, jlong modTimeMillis) {
    jclass cls = (*env)->GetObjectClass(env, context);
    if (cls == NULL) return JNI_FALSE;
    jmethodID getContentResolver = (*env)->GetMethodID(env, cls, "getContentResolver", "()Landroid/content/ContentResolver;");
    if (getContentResolver == NULL) { (*env)->DeleteLocalRef(env, cls); return JNI_FALSE; }
    jobject resolver = (*env)->CallObjectMethod(env, context, getContentResolver);
    if ((*env)->ExceptionCheck(env) || resolver == NULL) { (*env)->ExceptionClear(env); (*env)->DeleteLocalRef(env, cls); return JNI_FALSE; }

    jclass uriClass = (*env)->FindClass(env, "android/net/Uri");
    jmethodID parseMethod = (*env)->GetStaticMethodID(env, uriClass, "parse", "(Ljava/lang/String;)Landroid/net/Uri;");
    jstring juriStr = (*env)->NewStringUTF(env, uriStr);
    jobject uri = (*env)->CallStaticObjectMethod(env, uriClass, parseMethod, juriStr);
    (*env)->DeleteLocalRef(env, juriStr);
    if ((*env)->ExceptionCheck(env) || uri == NULL) { (*env)->ExceptionClear(env); ... cleanup ... return JNI_FALSE; }

    jclass resolverClass = (*env)->GetObjectClass(env, resolver);
    jmethodID openFD = (*env)->GetMethodID(env, resolverClass, "openFileDescriptor", "(Landroid/net/Uri;Ljava/lang/String;)Landroid/os/ParcelFileDescriptor;");
    jstring mode = (*env)->NewStringUTF(env, "rw");
    jobject pfd = (*env)->CallObjectMethod(env, resolver, openFD, uri, mode);
    (*env)->DeleteLocalRef(env, mode);
    if ((*env)->ExceptionCheck(env) || pfd == NULL) { (*env)->ExceptionClear(env); ... cleanup ... return JNI_FALSE; }

    jclass pfdClass = (*env)->GetObjectClass(env, pfd);
    jmethodID getFd = (*env)->GetMethodID(env, pfdClass, "getFd", "()I");
    int fd = (*env)->CallIntMethod(env, pfd, getFd);

    jboolean success = JNI_FALSE;
    if (fd >= 0) {
        long sec = modTimeMillis / 1000;
        long nsec = (modTimeMillis % 1000) * 1000000;
        struct timespec ts[2];
        ts[0].tv_sec = sec; ts[0].tv_nsec = nsec;
        ts[1].tv_sec = sec; ts[1].tv_nsec = nsec;
        if (futimens(fd, ts) == 0) success = JNI_TRUE;
    }

    jmethodID closePfd = (*env)->GetMethodID(env, pfdClass, "close", "()V");
    (*env)->CallVoidMethod(env, pfd, closePfd);
    ... cleanup all local refs ...
    return success;
}
```

### 3. Go: wrapper `setModTime`

**Файл:** `call_static_android.go` — добавить Go wrapper:

```go
func setModTime(uri fyne.URI, mtime time.Time) (bool, error) {
    if uri == nil {
        return false, fmt.Errorf("uri is nil")
    }
    modTimeMs := mtime.UnixMilli()
    ok, err := callBooleanStringLong("setModTime", uri.String(), modTimeMs)
    if err != nil {
        return false, err
    }
    if ok {
        return true, nil
    }
    ok2, err2 := setModTimeUsingFDNative(uri.String(), modTimeMs)
    return ok2, err2
}
```

### 4. Раскомментировать `progress.go:470-477`

```go
if err == nil {
    t, err := fileModTime(source.Name())
    log.Debugf("source ModTime %s %v:%v", source.Name(), t, err)
    setModTime(destination.URI(), t)
}
```

**Статус:** ПРИМЕНЕНО. Оба подхода (Java + FD) возвращают false для
`com.android.externalstorage.documents`. Это ограничение Android SAF.

### 5. Результат: setModTime не работает через DocumentsContract

**Подтверждено логами (API 36):**
- Java `ContentResolver.update` на DocumentsContract URI: `UnsupportedOperationException`
- C `futimens`: fd открывается (fd=106), но `errno=13` (EACCES) для external storage, `errno=1` (EPERM) для MediaStore

**Следующий шаг:** попробовать MediaStore API с правильными колонками и единицами:
- `DATE_MODIFIED` — в **секундах** (мы передавали миллисекунды — баг!)
- `DATE_TAKEN` — в миллисекундах, для фото/видео более writable
- Для DocumentsContract URI — resolve через file path к MediaStore URI
- Для `content://media/external/downloads/` — использовать напрямую

---

# Сравнение со старой версией 1.11.61

Старая версия использовала inline C JNI (`count_children_android.go`, `get_children_uri_android.go`).
Всё работало корректно: имена файлов, mod time, Chtimes.

## Соответствие старый C → новый Java

| Старый C файл | Новый Java метод | Статус |
|---|---|---|
| `GetFileName()` → DISPLAY_NAME query | `getFileName()` DISPLAY_NAME + tree fallback | Fix 1a: порядок восстановлен |
| `GetModTime()` → last_modified/date_modified | `getModTime()` separate try/catch | Fix 2: применён |
| `CountChildren()` | `countChildren()` buildChildDocumentsUri | Fix 3: применён |
| `GetChildrenURI()` + `GetChildrenList()` | `getChildrenURIs()` + `processChildrenCursor()` | Fix 4+5: применён |

## Замеченные различия

1. `.part` файл (`0f104086...part`) — SAF провайдер возвращает 8 children для него.
   Старая версия: `walk deep 2` (ограничение рекурсии). Новая: то же поведение.
2. `Chtimes` работает только для локальных файлов (`/data/...`). Для content:// destination
   `setModTime` закомментирован (`progress.go:468-475`) и удалён в Stage 3.

# Этап: Убрать Java setModTime — оставить только futimens

## Факты

- `ContentResolver.update()` → `UnsupportedOperationException` на ВСЕХ URI (DocumentsContract + MediaStore)
- `futimens` через FD → работает на Android 9
- Java `setModTime` бесполезен — только генерирует исключения

## План — 2 файла

### 1. `GoNativeActivity.java` — удалить метод `setModTime` (строки 523-541)

Удалить целиком блок:
```java
static boolean setModTime(String uriStr, long modTimeMillis) { ... }
```

### 2. `call_static_android.go` — убрать Java вызов из Go `setModTime` (строка 668)

Заменить:
```go
func setModTime(uri fyne.URI, mtime time.Time) {
	if uri == nil {
		return
	}
	if uri.Scheme() == "file" {
		log.Debugf("Chtimes %s %v: %v", uri.Path(), mtime,
			os.Chtimes(uri.Path(), time.Time{}, mtime))
		return
	}
	if uri.Scheme() != "content" {
		return
	}
	modTimeMs := mtime.UnixMilli()
	ok, err := callBooleanStringLong("setModTime", uri.String(), modTimeMs)
	log.Debugf("setModTime Java %s %v: %v %v", uri, mtime, ok, err)
	if ok {
		return
	}
	ok2 := setModTimeUsingFD(uri.String(), modTimeMs)
	log.Debugf("setModTime FD %s: %v", uri, ok2)
}
```

На:
```go
func setModTime(uri fyne.URI, mtime time.Time) {
	if uri == nil {
		return
	}
	if uri.Scheme() == "file" {
		log.Debugf("Chtimes %s %v: %v", uri.Path(), mtime,
			os.Chtimes(uri.Path(), time.Time{}, mtime))
		return
	}
	if uri.Scheme() != "content" {
		return
	}
	modTimeMs := mtime.UnixMilli()
	ok := setModTimeUsingFD(uri.String(), modTimeMs)
	log.Debugf("setModTime FD %s %v: %v", uri, mtime, ok)
}
```

---

## Предыдущий этап: Рефакторинг завершён (незакоммичен)

18 файлов, -3177/+1114 строк. Все фиксы 1-7 + setModTime + логирование применены.
