# Fix: Remove invalid `arch` field + add `output` to F-Droid metadata

## Problem
F-Droid CI fails with:
```
MetaDataException: Unrecognised build flag 'arch' in 'metadata/com.github.abakum.crocson.yml'
```

## Fix

### 1. `metadata/com.github.abakum.crocson.yml`
- Remove all 4 `arch:` lines (38, 55, 72, 89)
- Add `output: crocson.apk` to each of the 4 build entries

### 2. `.github/workflows/fdroid.yml` Python template (~line 74-91)
- Remove `f"    arch: {arch}\n"` line
- Add `f"    output: crocson.apk\n"` line

### 3. `.github/workflows/fyne.yml` Python template (~line 974-991)
- Remove `f"    arch: {arch}\n"` line
- Add `f"    output: crocson.apk\n"` line
